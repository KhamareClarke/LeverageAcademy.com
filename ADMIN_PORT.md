# Running Admin on Different Port

## To run student login (port 3000):
```bash
npm run dev
```
Access at: http://localhost:3000

## To run admin login (port 3001):
```bash
npm run dev:admin
```
Access at: http://localhost:3001

## Note
Both will run the same application but on different ports. This helps with session isolation during development.
